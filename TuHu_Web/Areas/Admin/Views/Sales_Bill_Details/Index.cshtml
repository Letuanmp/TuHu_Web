@model IEnumerable<TuHu_Web.Models.Sales_Bill_Details>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bill_Of_Sale.Id_Bill_Of_Sale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Name_Product)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price_Bill_Details)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bill_Of_Sale.Id_Bill_Of_Sale)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Name_Product)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price_Bill_Details)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Sales_Bill_Details }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Sales_Bill_Details }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Sales_Bill_Details })
        </td>
    </tr>
}

</table>
