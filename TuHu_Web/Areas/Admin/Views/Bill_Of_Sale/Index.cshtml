@model IEnumerable<TuHu_Web.Models.Bill_Of_Sale>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customer.Name_Customer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Staff.Name_Of_Staff)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date_And_Time)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.Name_Customer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Staff.Name_Of_Staff)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date_And_Time)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Bill_Of_Sale }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Bill_Of_Sale }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Bill_Of_Sale })
        </td>
    </tr>
}

</table>
